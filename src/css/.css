@charset "UTF-8";
@media (max-width: 1024px) and (min-width: 768px) {
  header {
    width: 100%;
    position: sticky;
    top: 0;
    z-index: 100;
    background: #fff;
    border-bottom: 1px solid #ccc;
    box-shadow: var(--shadow1);
  }
  header .login_join {
    width: 100%;
    margin: 0 auto;
    text-align: right;
    padding-top: 10px;
  }
  header .login_join a {
    margin-left: 10px;
    font-size: var(--fontsize-14);
  }
  header .h_inner {
    width: 100%;
    height: 60px;
    margin: 0 auto;
    position: relative;
    padding-top: 10px;
  }
  header .h_inner h1 a img {
    position: absolute;
    top: 15px;
  }
  header .h_inner .navi {
    width: 100vh;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
    top: 20px;
  }
  header .h_inner .navi .gnb {
    width: 500px;
    display: flex;
    justify-content: space-around;
    margin: 0 auto;
    background: rgba(255, 255, 255, 0.8);
    border-radius: 0px 0px 10px 10px;
    position: relative;
  }
  header .h_inner .navi .gnb li {
    text-align: center;
    font-weight: bold;
    position: relative;
  }
  header .h_inner .navi .gnb li .lnb {
    max-height: 0;
    opacity: 0;
    visibility: hidden;
    overflow: hidden;
    transition: all 0.8s;
    margin-top: 0;
    line-height: 50px;
    font-size: var(--fontsize-14);
  }
  header .h_inner .navi .gnb li .lnb li a {
    transition: 0.1s;
  }
  header .h_inner .navi .gnb li .lnb li a:hover {
    border-bottom: 2px solid #222;
    padding-bottom: 5px;
  }
  header .h_inner .navi .gnb li:hover .lnb {
    max-height: 500px;
    opacity: 1;
    visibility: visible;
    margin-top: 30px;
  }
  header .h_right {
    position: absolute;
    top: 20px;
    right: 10px;
  }
  header .h_right button {
    border: none;
    background: #fff;
  }
  header .h_right img {
    width: 40px;
    transform: translateY(-2px);
  }
  header .h_right .h_search, header .h_right .h_user {
    font-size: 30px;
    transform: translateY(-8px);
    color: var(--txt-color);
  }
  .search_box {
    width: 600px;
    margin: 0 auto;
    padding-bottom: 20px;
  }
  .search_box input {
    width: 600px;
    padding: 10px 0px;
    border: none;
    border-bottom: 2px solid var(--main-color2);
    font-size: 1rem;
  }
  .search_box .search_icon {
    padding-left: 10px;
  }
  /* 푸터서식 */
  footer {
    width: 100%;
    height: 200px;
    background: #fff;
    border-top: 1px solid #ccc;
    box-shadow: 0px -2px 4px rgba(0, 0, 0, 0.3);
    position: relative;
  }
  footer .f_inner {
    width: 1200px;
    margin: 0 auto;
    padding-top: 20px;
  }
  footer .f_inner .top_btn {
    background: none;
    border: none;
    position: fixed;
    bottom: 91px;
    right: 25px;
    z-index: 10000;
  }
  footer .f_inner .top_btn .topbtn {
    background: #fff;
    color: var(--main-color2);
    font-size: var(--fontsize-16);
    padding: 10px 13px;
    border: 3px solid var(--main-color2);
    border-radius: 50%;
  }
  footer .f_inner ul {
    width: 700px;
    display: flex;
    margin: 0 auto;
    transform: translateY(-30px);
    justify-content: space-evenly;
    padding-bottom: 50px;
  }
  footer .f_inner ul li {
    cursor: pointer;
  }
  footer .f_inner .f_sns {
    font-size: 30px;
    transform: translateY(25px);
  }
  footer .f_inner .f_sns .f_sns_icon {
    padding: 0px 8px;
    float: right;
    cursor: pointer;
  }
  footer .f_inner .f_info {
    font-size: var(--fontsize-12);
    line-height: 20px;
  }
  footer .f_inner address {
    font-size: var(--fontsize-12);
    font-style: normal;
  }
}/*# sourceMappingURL=.css.map */